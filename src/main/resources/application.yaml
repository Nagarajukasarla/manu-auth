server:
  port: 8081

spring:
  application:
    name: auth-service

  profiles:
    active: dev

eureka:
  instance:
    prefer-ip-address: true

  client:
    service-url:
      defaultZone: http://eureka:8761/eureka/